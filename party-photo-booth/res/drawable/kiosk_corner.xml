<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <size
        android:height="64dp"
        android:width="64dp" />

    <gradient
        android:centerX="100%"
        android:centerY="0%"
        android:endColor="@color/background_color"
        android:gradientRadius="@dimen/kiosk_mode_button_exit_size"
        android:startColor="@color/kiosk_corner_color"
        android:type="radial" />

</shape>