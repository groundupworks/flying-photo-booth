<?xml version="1.0" encoding="utf-8"?>
<resources>

    <dimen name="photo_thumb_size">300dp</dimen>
    <dimen name="photo_thumb_text_line_height">45dp</dimen>
    <dimen name="kiosk_spacing">24dp</dimen>
    <dimen name="photo_strip_shadow_size">6dp</dimen>

    <!-- This should be approximately photo_thumb_size * 2 / 3 -->
    <dimen name="photo_thumb_logo_height">200dp</dimen>

    <!-- This must be photo_thumb_size + kiosk_spacing * 2 -->
    <dimen name="photo_strip_content_width">348dp</dimen>

    <!-- This must be photo_strip_content_width + photo_strip_shadow_size -->
    <dimen name="photo_strip_width">354dp</dimen>

    <!-- Text sizes -->
    <dimen name="text_size_small">24dp</dimen>
    <dimen name="text_size_normal">32dp</dimen>
    <dimen name="text_size_large">80dp</dimen>

</resources>