<?xml version="1.0" encoding="utf-8"?>
<resources>

    <dimen name="photo_thumb_size">200dp</dimen>
    <dimen name="photo_thumb_text_line_height">30dp</dimen>
    <dimen name="kiosk_spacing">12dp</dimen>
    <dimen name="photo_strip_shadow_size">6dp</dimen>

    <!-- This should be approximately photo_thumb_size * 2 / 3 -->
    <dimen name="photo_thumb_logo_height">133dp</dimen>

    <!-- This must be photo_thumb_size + kiosk_spacing * 2 -->
    <dimen name="photo_strip_content_width">224dp</dimen>

    <!-- This must be photo_strip_content_width + photo_strip_shadow_size -->
    <dimen name="photo_strip_width">230dp</dimen>

    <!-- Text sizes -->
    <dimen name="text_size_small">18dp</dimen>
    <dimen name="text_size_normal">24dp</dimen>
    <dimen name="text_size_large">60dp</dimen>

</resources>